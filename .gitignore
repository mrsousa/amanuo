# Segredos e credenciais
config.json
dados/token_cache.bin

# dados/contracts.json e dados/contexts/ SÃO versionados por enquanto
# (decisão consciente — revisar quando a carteira crescer ou tiver dado
# mais sensível).

fontes/   # documentos-fonte; ajuste se mudar "fontes_dir" em config.json

# Python
__pycache__/
*.pyc
.venv/
venv/
